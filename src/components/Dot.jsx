import React from 'react';
import classes from './UI/Dot.module.css';

const Dot = () => {
    return (
        <div className={classes.dot}>
            
        </div>
    );
};

export default Dot;
